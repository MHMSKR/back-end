const passport = require('passport');
const { db } = require('../../config/database');

// module.exports.enegyCheckById = (req, res,next) => {
//     const id = req.param.id;
//     res.send(id)
// }